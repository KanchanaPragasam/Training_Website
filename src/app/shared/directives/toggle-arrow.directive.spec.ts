import { ToggleArrowDirective } from './toggle-arrow.directive';

describe('ToggleArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleArrowDirective();
    expect(directive).toBeTruthy();
  });
});
